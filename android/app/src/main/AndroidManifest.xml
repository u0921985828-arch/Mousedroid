<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- El movil se anuncia como raton/teclado HID. BLUETOOTH_CONNECT se pide en
         caliente desde Android 12; por debajo bastan las dos antiguas. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Hacerse visible es "anunciarse", y desde Android 12 eso es ADVERTISE y no
         CONNECT. Sin este permiso ACTION_REQUEST_DISCOVERABLE lanza SecurityException,
         el catch se la tragaba y el movil no se hacia visible nunca: el emparejado
         inicial por Bluetooth no podia completarse y no habia ni un error a la vista. -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- allowBackup en false desde que hay un secreto guardado: con la copia
         activada, `adb backup` o la copia en la nube se llevan el codigo de
         emparejado en claro. -->
    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_compass"
        android:label="USB Mouse"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".PowerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
